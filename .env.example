# For Docker Compose: copy this file to ".env" in the same folder as docker-compose.yml
#   Windows CMD: copy .env.example .env
# Compose substitutes ${GEMINI_API_KEY} and ${GEMINI_MODEL} for the backend service.

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
