# For Docker Compose: copy this file to ".env" in the same folder as docker-compose.yml
#   Windows CMD: copy .env.example .env
# LLM providers are configured per-user via the AI Settings page in the UI.
# Add server-level defaults here if needed.

OPENAI_API_KEY=
GROQ_API_KEY=
